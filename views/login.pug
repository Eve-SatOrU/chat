meta(charset="utf-8")
meta(name="viewport", content="width=device-width,initial-scale=1,shrink-to-fit=no")
title login
link(href="/css/particles.css", rel="stylesheet")
link(href="/css/auth.css", rel="stylesheet")
#tsparticles
main.box
  h2 Login
  form
    .inputBox
      label(for="userName") Username
      input#userName(type="text", name="userName", placeholder="type your username", required)
    .inputBox
      label(for="userPassword") Password
      input#userPassword(type="password", name="userPassword", placeholder="type your password", required)
    div
      button(type="submit", name, style="float: left;") Submit
      a.button(href="/register", style="float: left;") Register
footer.